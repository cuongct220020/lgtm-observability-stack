# Tempo (Grafana Trace Storage) values

tempo:
  storage:
    trace:
      backend: local
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: "0.0.0.0:4317"
        http:
          endpoint: "0.0.0.0:4318"
